<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>National Parks Bucket List 🏔️</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #2d5016 0%, #4a7c59 50%, #7ba05b 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            background: linear-gradient(135deg, #2d5016 0%, #3d6b2e 100%);
            border-radius: 20px;
            padding: 50px;
            text-align: center;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            margin-bottom: 40px;
            border: 3px solid #7ba05b;
        }
        
        h1 {
            color: #f4e4c1;
            font-size: 3.5em;
            margin-bottom: 15px;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.4);
        }
        
        .subtitle {
            color: #d4c5a9;
            font-size: 1.4em;
            font-style: italic;
        }
        
        .parks-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }
        
        .park-card {
            background: linear-gradient(135deg, #f9f5eb 0%, #e8dcc4 100%);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
            border: 2px solid #a88b5c;
            position: relative;
            overflow: hidden;
        }
        
        .park-card::before {
            content: '🏔️';
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 120px;
            opacity: 0.1;
        }
        
        .park-card:hover {
            transform: translateY(-8px) rotate(1deg);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
        }
        
        .park-name {
            font-size: 2em;
            color: #2d5016;
            margin-bottom: 15px;
            font-weight: bold;
        }
        
        .info-row {
            display: flex;
            align-items: center;
            margin: 12px 0;
            font-size: 1.1em;
            color: #4a4a4a;
        }
        
        .icon {
            font-size: 1.5em;
            margin-right: 12px;
            min-width: 30px;
        }
        
        .state {
            color: #2d5016;
            font-weight: bold;
        }
        
        .season-badge {
            display: inline-block;
            background: linear-gradient(135deg, #7ba05b 0%, #5d8a43 100%);
            color: white;
            padding: 8px 18px;
            border-radius: 25px;
            font-weight: bold;
            margin: 10px 0;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.2);
        }
        
        .attraction {
            background: #fff9e6;
            border-left: 4px solid #d4a574;
            padding: 15px;
            margin-top: 15px;
            border-radius: 8px;
            font-style: italic;
            color: #5d4e37;
        }
        
        .footer {
            text-align: center;
            color: #f4e4c1;
            margin-top: 50px;
            padding: 25px;
            font-size: 1.2em;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        
        .count {
            background: rgba(255, 255, 255, 0.2);
            padding: 10px 20px;
            border-radius: 10px;
            display: inline-block;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🏔️ National Parks Bucket List 🌲</h1>
            <p class="subtitle">Adventure Awaits in America's Greatest Treasures</p>
            <div class="count">
                <%= parks.length %> Parks to Explore
            </div>
        </header>
        
        <div class="parks-grid">
            <% for (let i = 0; i < parks.length; i++) { %>
            <div class="park-card">
                <div class="park-name"><%= parks[i].park_name %></div>
                
                <div class="info-row">
                    <span class="icon">📍</span>
                    <span class="state"><%= parks[i].state %></span>
                </div>
                
                <div class="season-badge">
                    🍂 Best in <%= parks[i].best_season %>
                </div>
                
                <div class="attraction">
                    <strong>Must-See:</strong> <%= parks[i].main_attraction %>
                </div>
            </div>
            <% } %>
        </div>
        
        <div class="footer">
            <p>🌍 Explore. Discover. Protect. 🌍</p>
            <p style="font-size: 0.9em; margin-top: 10px;">National Park Service - Since 1916</p>
        </div>
    </div>
</body>
</html>
```

---

## Quick Setup Checklist:

When your CloudFormation stack completes:

1. **SSH to workstation** and run the SQL commands above
2. **Update your GitHub repo:**
   - Replace `index.js` with the version above (update RDS endpoint)
   - Replace `index.ejs` with the version above
   - Make sure `appspec.yml` uses `ec2-user` and `AfterInstall`
   - Commit and push

3. **Deploy via CodePipeline** and visit:
```
   http://<ProductionIP>:3000
